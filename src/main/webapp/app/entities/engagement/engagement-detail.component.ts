import { Component, OnInit } from '@angular/core';
import { ActivatedRoute } from '@angular/router';
import { JhiDataUtils } from 'ng-jhipster';

import { IEngagement } from 'app/shared/model/engagement.model';

@Component({
  selector: 'jhi-engagement-detail',
  templateUrl: './engagement-detail.component.html',
})
export class EngagementDetailComponent implements OnInit {
  engagement: IEngagement | null = null;

  constructor(protected dataUtils: JhiDataUtils, protected activatedRoute: ActivatedRoute) {}

  ngOnInit(): void {
    this.activatedRoute.data.subscribe(({ engagement }) => (this.engagement = engagement));
  }

  byteSize(base64String: string): string {
    return this.dataUtils.byteSize(base64String);
  }

  openFile(contentType = '', base64String: string): void {
    this.dataUtils.openFile(contentType, base64String);
  }

  previousState(): void {
    window.history.back();
  }
}
